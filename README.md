# EarthboundNative

This repo exists only so pre-rename builds of EarthBound (older than the
EarthboundNative -> EarthboundUpdater rename) can still successfully check
for and install an update. Once updated, the new build points at
EarthboundUpdater directly.

No source code or ongoing releases live here beyond what's needed to
bridge old clients across the rename.
